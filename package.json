{
  "name": "business",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter ./frontend --filter ./backend dev",
    "build": "pnpm --filter ./backend build && pnpm --filter ./frontend build"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@prisma/client", "@prisma/engines", "bcrypt", "esbuild", "prisma", "vue-demi"]
  }
}
