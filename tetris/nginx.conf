server {
    listen 8080;
    server_name _;

    sendfile off;
    aio off;

    # Required for SharedArrayBuffer (Ebiten WASM audio)
    add_header Cross-Origin-Opener-Policy "same-origin";
    add_header Cross-Origin-Embedder-Policy "require-corp";

    # Gzip compression (WASM compresses very well)
    gzip on;
    gzip_types application/wasm text/javascript application/javascript;
    gzip_min_length 1024;
    gzip_comp_level 6;

    root /usr/share/nginx/html;
    index index.html;

    # Serve .wasm with the correct MIME type
    types {
        text/html                 html htm;
        text/javascript           js;
        application/wasm          wasm;
    }

    # Cache WASM and JS assets for 7 days
    location ~* \.(wasm|js)$ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=604800, immutable";
        add_header Cross-Origin-Opener-Policy "same-origin";
        add_header Cross-Origin-Embedder-Policy "require-corp";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
